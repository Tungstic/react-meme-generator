/* function MemeImage(props) {
  if (props.top) {
    return (
      <img
        alt="meme"
        data-test-id="meme-image"
        src={`${baseURL}${props.name}/${props.top}.jpg`}
      />
    );
  }
}

function PreviewImage() {
  return (
    <img alt="meme" data-test-id="meme-image" src={`${baseURL}preview.jpg`} />
  );
} */
